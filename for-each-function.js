


const numbers = [1,2,3,4,5];

numbers.forEach((numbers, index) => {
    console.log(`Elemeny at index ${index} is ${numbers}`);
}

)

// Another example

const friends = ["Sunny" , "Sadanand" , "Naari" , "Purush"]

friends.forEach((friends,index) =>{
    console.log(`My Friend ${friends} lives in the row house number ${index} `);
}

)

const rank = ["Rohan","CHagan" , "Magan" ,"Bhajan"] 

rank.forEach((rank,index) =>{
    console.log(`${rank} secure the rank ${index}`);
}


)